export const PRODUCTS_KEY = 'products-loading';
export const PRODUCT_TYPES = ['GOODS','SERVICE'] as const;
export const UNITS = ['PCS','KG','MTR','LTR','BOX','HRS','NOS','SET'] as const;

// ── GST rate options (common Indian GST slabs) ──────────────
export const GST_RATES = [0, 5, 12, 18, 28];

